/**
 * Re-export from the engine's env-config.
 * This stub exists so that the root Jest moduleNameMapper (@/ -> src/)
 * can resolve @/lib/env-config used by engine modules during testing.
 */
export { paths } from '../../services/engine/src/lib/env-config';
