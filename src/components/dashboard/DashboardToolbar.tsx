"use client";

// Toolbar placeholder — PDF/print/subscribe features removed (blocked deps).
// Kept as a component stub in case future toolbar actions are added.

export function DashboardToolbar() {
  return null;
}
